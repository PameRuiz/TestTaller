@model List<TestRazor.ViewModel.Clientes>
@{
    ViewBag.Title = "Listar";
}

<h2>Listar</h2>

@foreach(var cliente in Model)
{
    <label>@cliente.Apellido</label>
    <label>@cliente.Nombre</label>
    <br />
}